org: implementing
service: hello-world-lambda

provider:
  name: aws
  runtime: nodejs20.x
  region: eu-west-2

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: GET
          path: /hello

build:
  esbuild:
    bundle: true